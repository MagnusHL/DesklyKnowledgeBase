{% sw_extends '@Storefront/storefront/base.html.twig' %}

{% block base_head %}
    {{ parent() }}
    {% sw_include '@DesklyKnowledgeBase/storefront/page/kb-jsonld.html.twig' %}
{% endblock %}

{% block base_breadcrumb %}
    <div class="breadcrumb cms-breadcrumb container">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb" itemscope itemtype="https://schema.org/BreadcrumbList">
                <li class="breadcrumb-item" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                    <a href="{{ path('frontend.home.page') }}" itemprop="item">
                        <span itemprop="name">{{ "general.homeLink"|trans|striptags }}</span>
                    </a>
                    <meta itemprop="position" content="0">
                </li>
                <li class="breadcrumb-item" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                    <a href="{{ path('frontend.kb.overview') }}" itemprop="item">
                        <span itemprop="name">{{ "deskly-kb.breadcrumb.help"|trans }}</span>
                    </a>
                    <meta itemprop="position" content="1">
                </li>
                <li class="breadcrumb-item active" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem" aria-current="page">
                    <span itemprop="name">{{ page.category.name }}</span>
                    <meta itemprop="position" content="2">
                </li>
            </ol>
        </nav>
    </div>
{% endblock %}

{% block base_content %}
    <div class="cms-page deskly-kb-category-page">

        {# Sektion 1: Header (weiss) #}
        <section class="deskly-kb-category-hero">
            <div class="container">
                <a href="{{ path('frontend.kb.overview') }}" class="deskly-kb-back-link">
                    &larr; {{ "deskly-kb.category.backToOverview"|trans }}
                </a>

                <h1>{{ page.category.name }}</h1>

                {% if page.category.description %}
                    <p class="deskly-kb-category-hero__description">
                        {{ page.category.description|raw }}
                    </p>
                {% endif %}
            </div>
        </section>

        {# Sektion 2: Artikel (hellgrau) #}
        {% if page.articles is not empty %}
            <section class="deskly-kb-articles">
                <div class="container">
                    <div class="deskly-kb-article-list">
                        {% for article in page.articles %}
                            <a href="{{ path('frontend.kb.article', { categorySlug: page.category.slug, articleSlug: article.slug }) }}"
                               class="deskly-kb-article-list__item"
                               title="{{ article.title }}">
                                <div class="deskly-kb-article-list__content">
                                    <h2 class="deskly-kb-article-list__title">{{ article.title }}</h2>

                                    {% if article.shortText %}
                                        <p class="deskly-kb-article-list__text">
                                            {{ article.shortText|striptags|u.truncate(200, '...') }}
                                        </p>
                                    {% endif %}

                                    {% if article.tags is not empty %}
                                        <div class="deskly-kb-article-list__tags">
                                            {% for tag in article.tags %}
                                                <span class="deskly-kb-article-list__tag">{{ tag }}</span>
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                </div>

                                <span class="deskly-kb-article-list__arrow" aria-hidden="true">&rsaquo;</span>
                            </a>
                        {% endfor %}
                    </div>
                </div>
            </section>
        {% else %}
            <div class="container">
                <div class="alert alert-info">
                    {{ "deskly-kb.category.noArticles"|trans }}
                </div>
            </div>
        {% endif %}

    </div>
{% endblock %}
